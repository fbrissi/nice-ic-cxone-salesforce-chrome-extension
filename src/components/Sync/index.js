import React from 'react';
import './style.css';

const Sync = () => (
  <div>
    <button
      type="button"
      className="header-sync"
      aria-label="Refresh"
    />
  </div>
);

export default Sync;
